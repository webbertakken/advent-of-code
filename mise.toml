[tools]
node = "22.11.0"
yarn = "4.14.1"
actionlint = "latest"
shellcheck = "latest"
gitleaks = "latest"
# Required by `2024/typescript/wasm_optimisations/`. The wasm-pack build
# step in `.github/workflows/checks.yml` produces `wasm_optimisations/pkg/`
# which the tests in `2024/typescript/tests/day07-wasm.test.ts` import.
# rust = "stable"  # provided by GitHub Actions runners; not pinned here
# wasm-pack = "latest"  # installed via cargo in CI
